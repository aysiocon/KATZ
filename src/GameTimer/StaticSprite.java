package GameTimer;

import javafx.scene.image.Image;

public abstract class StaticSprite extends Sprite{

	public StaticSprite(double xPos, double yPos, Image image) {
		super(xPos, yPos, image);
	}

}
